apiVersion: moiraweave.io/v1alpha1
kind: Workload
metadata:
  name: echo-model
spec:
  type: model-service
  image: local/mock-model:e2e
  endpoint: http://mock-model:8080/v2/models/echo/infer
  execution:
    mode: async
    timeoutSeconds: 60
  ports:
    - name: http
      port: 8080
